/**
 * Player state machine states.
 * Used by the Shadow Creature player entity.
 */
const PlayerState = {
	IDLE: 'idle',
	MOVING: 'moving',
	ATTACKING: 'attacking',
	DODGING: 'dodging',
	HIT: 'hit',
	DYING: 'dying',
};

export default PlayerState;

